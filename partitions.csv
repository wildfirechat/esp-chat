# Cut for where this project ends up, not where it is today (ASSESSMENT.md 10.4).
#
# Changing this table means a full erase and reflash, which throws away the
# SQLite database and the WiFi calibration in NVS -- so it was cut once at P0
# for the audio-call build rather than for the protocol demo, and re-cut once,
# here, when phase two ran the app out of room (ASSESSMENT.md 8.13).
#
# The slots were 5M each. The app reached 4.99 MB with friend and group
# management in it -- 13 KB of headroom, which is not headroom -- so both go to
# 5.5M and storage gives up the 1 MB. The 216 KB that used to sit unused past
# the end of storage goes in at the same time, which is where the round 4992K
# comes from: this table now fills the 16 MB exactly.
#
# storage/ is the FATFS volume the SQLite database lives on (/wfc/wfc.db). It
# holds messages, the sync heads and the profile caches, none of which is
# anywhere near 4.8 MB -- the retention caps in wfc-esp/README.md are the real
# limit on what it holds, not this number.
#
# Name,     Type, SubType, Offset,  Size,  Flags
nvs,        data, nvs,     ,        24K,
otadata,    data, ota,     ,        8K,
phy_init,   data, phy,     ,        4K,
ota_0,      app,  ota_0,   ,        5632K,
ota_1,      app,  ota_1,   ,        5632K,
storage,    data, fat,     ,        4992K,
